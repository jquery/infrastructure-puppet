# This file manages external Puppet Forge modules available on our setup.
# In general we avoid using third-party moduels due to their extra complexity,
# but there are a few cases where using them makes sense.
# Documentation here: https://github.com/xorpaul/g10k

moduledir 'vendor_modules'

mod 'puppetlabs/sshkeys_core', '2.5.1'

mod 'puppetlabs/stdlib', '9.7.0'
mod 'puppetlabs/apt', '11.3.2'
mod 'puppetlabs/concat', '9.1.0'

mod 'puppet/dnsquery', '5.1.0'
