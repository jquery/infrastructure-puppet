profile::puppet::agent::puppet_server: puppet-03.stage.ops.jquery.net
profile::puppet::server::git_branch: staging

# TODO: change me
profile::certbot::email: jquery-letsencrypt@taavi.wtf

profile::codeorigin::vhost_hostnames:
  - code2.jquery.com
  - "%{::facts.fqdn}"

profile::codeorigin::certificate_names:
  - "%{::facts.fqdn}"
