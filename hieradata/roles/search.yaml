# Overrides for any 'search' hosts
# - /hierdata/common.yaml
# - /hierdata/roles/search.yaml [THIS FILE]
# - /hierdata/environments/*/common.yaml
# - /hierdata/environments/*/roles/search.yaml [unused]

profile::certbot::certificates:
  search:
    domains:
      - typesense.jquery.com
      - "%{::facts.fqdn}"

profile::typesense::tls_key_name: search
