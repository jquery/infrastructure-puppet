:cachedir: '/tmp/g10k'

sources:
  jquery-puppet:
    remote: 'https://github.com/jquery/infrastructure-puppet'
    basedir: '<%= @g10k_deploy_base_path %>'

    filter_regex: '<%= @g10k_branch_filter %>'
    force_forge_versions: true
